{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Remote launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/project.out",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build with g++",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
